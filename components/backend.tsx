import * as React from "react";

interface IBackendProps {}

const Backend: React.FunctionComponent<IBackendProps> = (props) => {
  return <aside className='relative'></aside>;
};

export default Backend;
